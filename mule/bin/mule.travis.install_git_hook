#! /bin/bash


if [[ -z "$SWEET_ROOT" ]]; then
	echo "SWEET_ROOT environment variable not found!"
	exit 1
fi

echo "Changing to hooks directory"
cd $SWEET_ROOT/.git/hooks || exit 1

echo "Installing hooks"
ln ../../mule/git_hooks/pre-commit  ./ -fs || exit 1

echo "Done..."
